version: '3'
services:

  test:
    tty: true
    build:
      context: .
      dockerfile: Dockerfile
    command: npm run test

  test_watch:
    tty: true
    build:
      context: .
      dockerfile: Dockerfile
    command: npm run test:watch