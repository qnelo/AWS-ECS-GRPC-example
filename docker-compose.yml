version: '3'
services:

  app:
    tty: true
    build:
      context: .
      dockerfile: Dockerfile
    command: npm run test

  test_docker:
    tty: true
    build:
      context: .
      dockerfile: DockerfileTest
    volumes:
      - .:/src/app
    command: npm run test:watch