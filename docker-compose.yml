version: '3'
services:

  build:
    tty: true
    build:
      context: .
      dockerfile: Dockerfile
    command: npm run test

  test_watch:
    tty: true
    build:
      context: .
      dockerfile: DockerfileTest
    command: npm run test:watch